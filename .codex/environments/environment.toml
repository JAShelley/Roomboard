# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "New project"

[setup]
script = ""

[[actions]]
name = "Run"
icon = "run"
command = '''
You are working on a Chrome extension that extracts veterinary diagnostic data (CBC, CHEM, etc.) from EzyVet pages and builds a time-series table and charts.

Your goal is to TEST, DEBUG, and IMPROVE the extension until it is stable and produces correct structured outputs.

## CONTEXT
- The extension scans DOM tables and extracts analytes across multiple diagnostic tests.
- It builds a dataset where:
  - X-axis = test dates
  - Y-values = analyte values
- Files likely involved:
  - content.js (DOM scraping + parsing)
  - background.js (or service worker)
  - UI / injected panel logic
  - any helper parsing or charting utilities

## TASK OBJECTIVES

### 1. Create a Test Strategy
- Identify all core behaviors that must be validated:
  - Detect diagnostic test sections (CBC, CHEM, etc.)
  - Extract analyte names correctly
  - Extract numeric values and units
  - Associate values with correct test dates
  - Handle missing / non-numeric values
  - Handle multiple tests over time
- Define edge cases:
  - Inconsistent table formats
  - Hidden or dynamically loaded elements
  - Duplicate analytes across panels
  - Null / abnormal values (e.g., “>”, “<”, text)

### 2. Build Automated Tests
- Add a test framework if missing (prefer lightweight: Jest or Playwright)
- Create mock HTML fixtures simulating EzyVet pages
- Write tests that:
  - Validate correct parsing of each diagnostic type
  - Validate time-series aggregation
  - Validate output structure for charting
- Ensure tests are deterministic and reproducible

### 3. Run Tests and Identify Failures
- Execute all tests
- Identify:
  - Parsing errors
  - Incorrect mappings (date ↔ analyte)
  - Data type inconsistencies
  - DOM selection failures

### 4. Fix and Improve Code
- Refactor parsing logic to be:
  - Modular (separate extraction, normalization, aggregation)
  - Robust to DOM variation
- Improve:
  - Selector reliability (avoid brittle selectors)
  - Data normalization (numbers, units, strings)
  - Error handling and fallbacks
- Ensure extension does not break on partial data

### 5. Validate End-to-End Behavior
- Simulate full workflow:
  - Load fixture HTML
  - Run content script
  - Generate final dataset
- Confirm:
  - Correct analytes across dates
  - No duplicated or missing values
  - Clean structure for chart rendering

### 6. Upgrade Features (if stable)
- Add:
  - Configurable analyte whitelist
  - Improved labeling of panels (CBC, CHEM, etc.)
  - Sorting by date
  - Optional normalization of units

### 7. Output Requirements
- Show:
  - Summary of issues found
  - List of fixes applied
  - Test coverage summary
- Provide:
  - Clean git-style diff of all changes
  - Any new test files created

## CONSTRAINTS
- Do NOT remove existing functionality unless broken
- Keep extension lightweight (no heavy frameworks)
- Prefer pure JS and minimal dependencies
- Ensure Chrome extension compatibility (Manifest V3)

## SUCCESS CRITERIA
- All tests pass
- Data extraction is consistent across fixtures
- Output dataset is correct and chart-ready
- Code is modular and maintainable

## EXECUTION LOOP
- Plan → Implement → Run tests → Fix → Repeat
- Continue until all tests pass and no major issues remain
'''
